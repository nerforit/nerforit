import PublicShell from "@/components/PublicShell";import {articles} from "@/lib/demo";import Link from "next/link";export default function Articles(){return <PublicShell><section className="section"><div className="container"><div className="eyebrow">Journal</div><h1>Articles</h1><div className="article-grid" style={{marginTop:35}}>{articles.map(a=><article className="article" key={a.slug}><div className="small">NERFORIT</div><h3>{a.title}</h3><p className="muted">{a.excerpt}</p><Link href={"/articles/"+a.slug}>Read →</Link></article>)}</div></div></section></PublicShell>}