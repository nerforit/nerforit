import PublicShell from "@/components/PublicShell";import {articles} from "@/lib/demo";export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(x=>x.slug===slug)||articles[0];return <PublicShell><section className="section"><div className="container" style={{maxWidth:800}}><div className="eyebrow">Article</div><h1>{a.title}</h1><p className="muted">{a.excerpt}</p><hr/><p style={{lineHeight:1.9}}>This starter content will be managed from the admin panel and stored in Supabase.</p></div></section></PublicShell>}