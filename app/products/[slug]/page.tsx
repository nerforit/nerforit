import PublicShell from "@/components/PublicShell";import {products} from "@/lib/demo";export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug)||products[0];return <PublicShell><section className="detail"><div className="container detail-grid"><div className="detail-image">◫</div><div><div className="eyebrow">{p.category}</div><h1>{p.name}</h1><div className="price">Rp {p.price.toLocaleString("id-ID")}</div><p className="muted">Product information will be connected to Supabase.</p><button className="btn">Add to cart</button></div></div></section></PublicShell>}