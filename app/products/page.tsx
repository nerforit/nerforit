import PublicShell from "@/components/PublicShell";import ProductCard from "@/components/ProductCard";import {products} from "@/lib/demo";export default function Products(){return <PublicShell><section className="section"><div className="container"><div className="eyebrow">Store</div><h1>Products</h1><div className="grid" style={{marginTop:35}}>{products.map(p=><ProductCard key={p.id} p={p}/>)}</div></div></section></PublicShell>}